command to run this :=-python train_model.py
                        python -m streamlit run app.py

# palm_m
This is palm reader which tells about the head line, heart line and life line with local chatbot which assist a person . The UI is made in streamlit, and python
